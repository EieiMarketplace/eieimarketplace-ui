import { Market } from "@/shared/interface";

export default function MarketReservationSubmitCard({
  market,
}: {
  market: Market;
}) {
  return <div className="h-[400px] w-full bg-amber-100"></div>;
}
